query UserProfile($userProfileId: Int!) {
  userProfile(id: $userProfileId) {
    id
    address
    age
    first_name
    gender
    last_name
    profile_img
    section {
      id
      section_name
    }
    disabled
    username
  }
}
