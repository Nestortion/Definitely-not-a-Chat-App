mutation RemoveMember($groupId: Int, $userId: Int) {
  removeMember(group_id: $groupId, user_id: $userId) {
    id
    first_name
    last_name
    section {
      id
      section_name
    }
    profile_img
    age
    gender
  }
}
