subscription GroupRolesUpdated($user: Int, $groupId: Int) {
  groupRolesUpdated(user: $user, group_id: $groupId) {
    newRoles {
      id
      role_name
      emoji
      description
      group_id
      role_type
      is_default
    }
    group_id
  }
}
