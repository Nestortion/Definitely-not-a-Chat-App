query OtherUser($groupId: Int) {
  otherUser(group_id: $groupId) {
    id
    first_name
    last_name
    address
    section {
      id
      section_name
      disabled
    }
    profile_img
    age
    gender
  }
}
