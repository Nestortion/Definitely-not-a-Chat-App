query SystemStats {
  systemStats {
    userCount
    groupCount
    pendingReportCount
    userChatsCount
    latestAdminLog {
      id
      user_id
      action_description
      full_name
      createdAt
    }
  }
}
