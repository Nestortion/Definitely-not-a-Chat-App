query GroupMembers($groupId: Int) {
  groupMembers(group_id: $groupId) {
    id
    username
    first_name
    address
    last_name
    profile_img
    age
    gender
    section {
      id
      section_name
      disabled
    }
  }
}
